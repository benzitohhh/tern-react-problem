{
  "name": "",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "node node_modules/watchify/bin/cmd.js     src/index.js -o public/build/index.js --debug --verbose",
    "build": "node node_modules/browserify/bin/cmd.js src/index.js -o public/build/index.js --debug --verbose"
  },
  "browserify": {
    "transform": [
      "babelify"
    ]
  },
  "dependencies": {
    "react": "^0.13.3"
  },
  "devDependencies": {
    "babelify": "^6.1.3",
    "browserify": "^11.0.0",
    "watchify": "^3.3.0"
  }
}
